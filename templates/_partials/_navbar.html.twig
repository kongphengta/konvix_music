<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
	<div class="container">

		<a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
			🎵 Konvix Music
		</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mainNavbar">
			<ul class="navbar-nav me-auto">
				<li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
				<li class="nav-item"><a class="nav-link" href="{{ path('app_song_index') }}">Chansons</a></li>
				<li class="nav-item"><a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a></li>
				<li class="nav-item"><a class="nav-link" href="{{ path('app_song_album', { album: 'konvix-country-vol1' }) }}">Albums</a></li>
				<li class="nav-item"><a class="nav-link" href="{{ path('app_song_search') }}">Recherche</a></li>
			</ul>

			<ul class="navbar-nav ms-auto align-items-center gap-2">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_profile') }}">
							👤 {{ app.user.firstName }}
							{% if app.user.subscriptionPlan == 'premium' %}
								<span class="badge bg-warning text-dark ms-1">Premium</span>
							{% else %}
								<span class="badge bg-success ms-1">Gratuit</span>
							{% endif %}
						</a>
					</li>
					{% if app.user.isArtist %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_artist_dashboard') }}">🎤 Mon espace</a>
						</li>
					{% endif %}
					<li class="nav-item">
						<a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
					</li>
					<li class="nav-item">
						<a class="btn btn-outline-light btn-sm" href="{{ path('app_register_artist') }}">🎤 Artiste</a>
					</li>
					<li class="nav-item">
						<a class="btn btn-success btn-sm fw-bold" href="{{ path('app_register') }}">🎧 Auditeur</a>
					</li>
				{% endif %}
			</ul>
		</div>

	</div>
</nav>
