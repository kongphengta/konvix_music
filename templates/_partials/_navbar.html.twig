<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">

        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            Konvix Music
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto">
                <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                <a class="nav-link" href="{{ path('app_songs') }}">Chansons</a>
                <a class="nav-link" href="{{ path('app_songs_category', { category: 'country' }) }}">Catégories</a>
                <a class="nav-link" href="{{ path('app_songs_album', { album: 'konvix-country-vol1' }) }}">Albums</a>
                <a class="nav-link" href="{{ path('app_songs_search') }}">Recherche</a>
              </ul>
            </div>

          </div>
        </nav>