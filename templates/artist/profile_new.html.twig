{% extends 'base.html.twig' %}

{% block title %}Créer mon profil artiste{% endblock %}

{% block body %}
    {% include '_partials/header.html.twig' %}

    <main>
        <section class="page-hero small-hero">
            <div class="container page-hero-inner">
                <div>
                    <span class="eyebrow">Artiste</span>
                    <h1>Créer mon profil</h1>
                </div>
                <a class="btn btn-secondary" href="{{ path('app_artist_dashboard') }}">Retour</a>
            </div>
        </section>

        <section class="section">
            <div class="container form-shell">
                {{ form_start(form) }}
                    <div class="form-grid">
                        <div>{{ form_row(form.slug) }}</div>
                        <div>{{ form_row(form.name) }}</div>
                        <div>{{ form_row(form.genre) }}</div>
                        <div>{{ form_row(form.city) }}</div>
                        <div>{{ form_row(form.followers) }}</div>
                        <div>{{ form_row(form.color) }}</div>
                        <div class="span-2">{{ form_row(form.bio) }}</div>
                    </div>

                    <div class="form-actions">
                        <button class="btn btn-primary" type="submit">Enregistrer mon profil</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </section>
    </main>

    {% include '_partials/footer.html.twig' %}
{% endblock %}
